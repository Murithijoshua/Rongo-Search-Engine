Replace your current path
loading.py
 path = "../results"


1. install virtualenv
    python3 -m venv venv
2. install dependencies
    pip install -r dependencies.tx
3.        export FLASK_APP=app
          flask run

          wait for 2-3 sec for indexing,
          enter your keyword inside the form
